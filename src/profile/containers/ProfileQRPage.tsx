import React from 'react';
import ProfileQRPage from '../components/ProfileQRPage';
import PageLayout from '../../layout/containers/PageLayout';

const ProfileQRPageContainer: React.FC = () => {
  return (
    <PageLayout>
      <ProfileQRPage />
    </PageLayout>
  );
};

export default ProfileQRPageContainer;
