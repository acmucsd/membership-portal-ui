/**
 * TODO - Delete this file.
 * This temporary reducer is only here for the purpose of setting up redux.
 * It will be deleted once the first reducer is created.
 */

const TempReducer = (state = {}, action) => {
  return state;
***REMOVED***

export default TempReducer;
